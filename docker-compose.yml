version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Runs uvicorn with reload enabled for live reloading during development
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
